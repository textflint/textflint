language: python
python: 
  - "3.7"
install: 
  - pip3 install --upgrade pip
  - pip3 install -r requirements.txt
  - pip3 install pytest
  - pip3 install travis-wait-improved
script:
  - travis-wait-improved --timeout 30m pytest
  
